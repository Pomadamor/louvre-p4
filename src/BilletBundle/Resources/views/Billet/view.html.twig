{% extends "BilletBundle::layout.html.twig" %}

{% block title %}
  Lecture d'une commande - {{ parent() }}
{% endblock %}

{% block billet_body %}

  <h2>{{ commande.id }}</h2>

  <p>
    <a href="{{ path('Billet_home') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('Billet_edit', {'id': commande.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'annonce
    </a>
    <a href="{{ path('Billet_delete', {'id': commande.id}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'annonce
    </a>
  </p>

{% endblock %}
