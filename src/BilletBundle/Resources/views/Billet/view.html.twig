{% extends "BilletBundle::layout.html.twig" %}

{% block title %}
  Lecture d'une annonce - {{ parent() }}
{% endblock %}

{% block billet_body %}

  <h2>{{ billet.title }}</h2>
  <i>Par {{ billet.author }}, le {{ billet.date|date('d/m/Y') }}</i>

  <div class="well">
    {{ billet.content }}
  </div>

  <p>
    <a href="{{ path('Billet_home') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('Billet_edit', {'id': billet.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'annonce
    </a>
    <a href="{{ path('Billet_delete', {'id': billet.id}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'annonce
    </a>
  </p>

{% endblock %}
